terraform {
    required_providers {
      azurerm = {
        source = "hashicorp/azurerm"
        version = "=3.0.0"
      }
    }
}

# configure microsoft Azure provider

provider "azurerm" {
  features {
    
  }
}